
## 📱 Что это за проект

**Namaz Profile (Qaza Tracker)** — mobile-first веб-приложение для учёта пропущенных намазов (каза).
Приложение работает **только на мобильных устройствах** и использует **Supabase** как backend.

---

## ⚙️ Стек

* Next.js (App Router)
* TypeScript
* Tailwind CSS
* Supabase (Auth + PostgreSQL + RLS)
* pnpm

---

## 📋 Требования

* Node.js **18+**
* pnpm
* Аккаунт в Supabase

---

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
pnpm install
```

---

### 2. Настройка переменных окружения

Создай файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

Данные берутся в Supabase:
**Settings → API**

---

### 3. Настройка базы данных

1. Открой Supabase Dashboard
2. Перейди в **SQL Editor**
3. Вставь содержимое файла:

```text
supabase-schema.sql
```

4. Выполни SQL-скрипт
   Он создаст:

* таблицы `users`, `prayer_stats`
* RLS-политику
* функции и триггеры

---

### 4. Запуск в dev-режиме

```bash
pnpm dev
```

Приложение будет доступно:

```
http://localhost:3000
```

⚠️ **Открывать с мобильного устройства**
(или через mobile view в браузере)

---

## 🔐 Аутентификация

Используется **Supabase Auth**:

* Email + Password
* Автоматическое создание профиля и статистики через SQL-функцию `handle_user_signup`

---

## 🧮 Логика подсчёта

* Старт намаза:

  * Мужчины: **12 лет**
  * Женщины: **9 лет**
* Начальное количество пропусков рассчитывается автоматически
* Учёт ведётся по каждому намазу отдельно

---

## 📦 Основные возможности

* Регистрация и вход
* Автоматический расчёт казов
* Учёт по каждому намазу
* Статистика:

  * всего пропущено
  * эквивалент в годах
* Mobile-only интерфейс

---

## 🧪 Сброс базы (опционально)

Для полного удаления таблиц и функций используй:

```sql
rollback.sql
```

⚠️ Удаляет **все данные**

---

## 🚢 Деплой

Проект можно развернуть на:

* Vercel
* Netlify
* любой Next.js-хостинг

Не забудь:

* добавить env-переменные
* проверить настройки Auth и CORS в Supabase
